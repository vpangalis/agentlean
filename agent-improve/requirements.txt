# Agent Improve — Python dependencies
# Aligned with agent-resolve — same stack, same versions

fastapi==0.128.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
pydantic==2.12.5
pydantic-settings==2.13.1

# LangGraph / LangChain
# Resolved against live PyPI 2026-08-21 (refactor step 2.3). Do not upgrade to
# a version written in a document — re-resolve with /verify-current-version.
# The rule is the floor: langgraph >=1.2.6, for "nested subgraph inherits
# parent checkpoint_ns (regression in 1.2.3)" — LangGraph 1.2.6, 2026-06-18.
langgraph==1.2.11
langchain==1.3.16
langchain-classic==1.0.8
langchain-openai==1.1.11
langchain-community==0.4.1
langchain-core==1.6.0
langchain-text-splitters==1.1.2
langsmith==0.7.3
langfuse==4.0.1

# Transitive, pinned because pip resolved them during the 2.3 upgrade:
#   langgraph-prebuilt 1.0.13 -> 1.1.0   (installed as a langgraph dependency;
#     CLAUDE.md §4.4 bans IMPORTING from langgraph.prebuilt, not its presence)
#   langgraph-sdk      0.3.14 -> 0.4.3
#   langchain-protocol 0.0.15 -> 0.0.18
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.3
langgraph-checkpoint==4.1.0
langchain-protocol==0.0.18

# Azure
azure-search-documents==11.6.0
azure-storage-blob==12.28.0
azure-identity==1.25.3
azure-core==1.38.0
openai==2.29.0

# Data / analysis
pandas==2.2.2
scipy==1.13.0
numpy==1.26.4
openpyxl==3.1.2
pyxlsb==1.0.10

# Utils
httpx==0.28.1
python-multipart==0.0.9
aiofiles==23.2.1

# Testing
pytest==9.0.3

# Type checking — DEV ONLY, not imported by any runtime module.
# Added 2026-08-31 with the commit-msg type-check gate
# (.claude/hooks/commit-msg-refactor-guard.py rule 3, config in mypy.ini).
# It earns its place because langgraph / langchain / langchain_core / langsmith
# / langchain_openai / pydantic all ship `py.typed`, so mypy checks calls
# against the REAL installed signatures rather than against Any.
mypy==2.3.1
