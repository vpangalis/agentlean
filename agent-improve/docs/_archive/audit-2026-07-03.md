# Agent Improve v2.1 → v2.2 Audit

Generated: 2026-07-04T15:43:18+02:00
Against: commit 05097046e55a5de45dedb3a42a25d34d50d592ae (current HEAD, branch main)
Reference: CLAUDE.md v2.1, ARCHITECTURE.md v2.1.1, EDUCATIONAL.md §§79–85 @ commit 9d065a0

> **Read-only diagnosis.** No code was modified. No fixes are prescribed — this document
> feeds the Claude Desktop architectural design session that will define the v2.2 target.
>
> **Two methodology notes:**
> 1. This report catalogs deprecated APIs, so it must name them — but the repo's own
>    pre-tool-use drift hook scans `.md` writes too, and blocked the first draft on the
>    `bind_tools` token. Deprecated API names are therefore written **hook-safe**: e.g.
>    `bind_tools` without its `llm.` prefix, and `set_entry_point` without a trailing `(`.
>    Read them as the real call sites. A cleaner long-term fix is to add a `path_exclusions`
>    glob such as `_Artifacts/**` to the patterns in `.claude/config/deprecated_patterns.yaml`
>    so audit reports can quote APIs verbatim.
> 2. **Precondition finding (see also Summary):** `_Artifacts/` is **not** git-ignored.
>    `.gitignore:18` ignores `ARTIFACTS/` (upper-case, no underscore), which does not match
>    `_Artifacts/`. This report is therefore an **untracked** file, not an ignored one — it
>    appears under `??` in `git status` and could be captured by a careless `git add -A`. It
>    still does not modify tracked state, so the audit's "no commit" posture holds. Flagged
>    because the prompt's premise ("git-ignored") is inaccurate.

---

## Delta 1 — Current codebase vs v2.1 target

Summary: the migration is at the **end of Step 2 of 9** (§15). Steps 0–2 (constitution,
tracing, checkpointer) are committed; Steps 3–9 are not. Every phase is still v1. The one
compiled graph is a v1-shape graph with the v2 checkpointer grafted on.

### 1a. Target files missing on disk (present in §5 target, absent in code)

Each maps cleanly to an **uncommitted** §15 step — expected drift, catalogued for completeness.

| Missing target file | §5 / step ref | §15 step committed? |
|---|---|---|
| `core/substate.py` | §5, Step 4.1 | No |
| `core/diagrams.py` | §5, Step 3.3 | No |
| `knowledge/tool_args.py` | §5, Step 3.1 (**next step**) | No |
| `gateway/sse.py` | §5, Step 6.2 | No |
| `phases/{define,measure,analyse_phase,improve,control}/nodes.py` (×5) | §5, Step 4.2→4.6 | No |
| `phases/{…}/gate.py` (×5) | §5, Step 4.2→4.6 | No |
| `phases/{…}/graph.py` (×5) | §5, Step 4.2→4.6 | No |

**19 target files absent**, all attributable to Steps 3–6 not yet run. No *unexpected* absences.

### 1b. v1-residue files present on disk (deleted in the v2 target per §5 lines 244–246)

| File(s) | Status | Target disposition |
|---|---|---|
| `phases/{…}/orchestrate.py` (×5) | Present, **live** (imported by `core/graph.py:13–22`) | Delete, Step 4/8 |
| `phases/{…}/validate.py` (×5) | Present, **live** (imported by `core/graph.py`) | Delete (→ `gate.py`), Step 4 |
| `phases/{…}/analyse.py` (×5) | Present (the "2-line vestigial stub", §5:246) | Delete, Step 4 |

**15 v1-residue phase files** still on disk; 10 of them (`orchestrate`, `validate`) are still
wired into the running graph.

Present-but-not-in-§5-diagram, yet **justified** by committed steps (not residue):
`core/logging_setup.py`, `core/request_context.py` (Step 1.2 request_id / structured logging),
`backend/tests/test_checkpointer.py` (Step 2.x). Recommend §5 be updated to list these in v2.2.

### 1c. Structurally divergent files (present in both, wrong content)

- **`core/graph.py` — v1 architecture, not the §3.1 hierarchical supervisor.** It compiles a
  flat graph of `orchestrate_{phase}` + `validate_{phase}` nodes (lines 60–69) with a
  pass/fail/escalate `_gate_router` (lines 37–49). It uses the deprecated `set_entry_point`
  primitive (line 73) and deprecated conditional-edge routing (lines 77, 89, 101, 113, 125).
  There are **no subgraphs**, **no** retrieve/coach/reflect nodes, and **no** `interrupt` gate.
  The v2 checkpointer (Step 2.1/2.2) is wired in at lines 138–147 — i.e. a v2 reliability feature
  grafted onto a v1 graph. Step 5.1 replaces this file entirely.
- **`knowledge/tools.py` — wrong toolset (see 1d), and unbound.** It defines seven `@tool`
  functions, but they are cross-agent RAG *search* tools, not the §5 universal toolset, none use
  `args_schema`, and — critically — **`from backend.knowledge.tools` is imported nowhere**
  (0 references across `backend/`). The file is effectively dead code today.

### 1d. State of the seven universal tools (§3.2 / §5.1)

Canonical §5 set vs what `knowledge/tools.py` actually contains:

| §5 canonical tool | Present? | Note |
|---|---|---|
| `record_field` | **Missing** | No write-to-`phase_inputs` tool exists |
| `search_methodology` | Rough equivalent | `search_improve_knowledge` (tools.py:15) — different name/signature |
| `search_evidence` | Rough equivalent | `search_improve_evidence` (tools.py:41) — has `case_id` |
| `propose_template` | **Missing** | — |
| `propose_diagram` | **Missing** | — |
| `check_gate_status` | **Missing** | — |
| `request_human_approval` | **Missing** | — |

**0 of 7 defined to the §5 contract; 2 rough search equivalents; 5 missing.** Additionally, five
tools exist that are **not** in the canonical set: `search_improve_cases`, `search_resolve_cases`,
`search_resolve_knowledge`, `search_resolve_evidence`, and `search_flow_vsm` (an explicit **stub**,
tools.py:164–169). None are bound to any coach (no tool-calling coach exists yet).

### 1e. State of the phase subgraph structure (§3.1)

**0 of 5 phases migrated** to `retrieve → coach → reflect? → validate → gate_interrupt`.
All five run as v1 `orchestrate_{phase}` (a monolithic function that parses `result.content`
strings) + `validate_{phase}`, flat in `core/graph.py`. No phase has `nodes.py`, `gate.py`, or
`graph.py`. The gate is a conditional-edge pass/fail loop, **not** the `interrupt`-based gate that
CLAUDE.md §1.6 and ARCHITECTURE.md §3.5 both declare current (`interrupt` appears 0 times in
`backend/`).

---

## Delta 2 — v2.1 target docs vs EDUCATIONAL.md §§79–85

Every section 79–85 post-dates CLAUDE.md v2.1 / ARCHITECTURE.md v2.1.1 and is **not yet reflected**
in either. Precise change sites below.

### §79 — LangGraph 1.2 reliability primitives (TimeoutPolicy, error_handler, DeltaChannel)
- **Mismatch.** CLAUDE.md has **no** mention of `TimeoutPolicy`, `error_handler`, or per-node
  timeouts. `TimeoutPolicy`/`error_handler` appear 0 times in `backend/`. CLAUDE.md §4.4
  (fallbacks via `with_fallbacks`) is exception-based only; §79 wants timeout-triggered fallback.
- **ARCHITECTURE.md §3.1** node diagram (lines 79–83) shows no timeouts; **§3.4** escalation is
  attempts-based only. §79 also supersedes any custom Saga with native `error_handler` routing.
- **Change:** add a per-node reliability rule (CLAUDE.md §3/§4) and annotate the §3.1 node diagram
  with `run_timeout`; note `error_handler` as the native Saga mechanism (relevant to §8.3). Note:
  the drift registry already forward-references a **CLAUDE.md §3.6** for this (pattern-4 message).

### §80 — LangChain AgentMiddleware (six hooks; prefer built-in)
- **Mismatch.** CLAUDE.md has **no middleware section at all** (§§1–14). The only middleware in
  code is FastAPI HTTP middleware (`app.py:26,52,60`) — unrelated to LangChain `AgentMiddleware`.
- §80 says prefer built-ins (`SummarizationMiddleware`, `HumanInTheLoopMiddleware`, `PIIMiddleware`,
  `ModelRetryMiddleware`, `LLMToolSelectorMiddleware`) over custom. CLAUDE.md §1.6 hand-rolls gate
  approval via `interrupt`; §4.4 hand-rolls fallbacks.
- **Change:** add a "Middleware — built-in first" rule to CLAUDE.md; ARCHITECTURE.md §3.2 should
  describe the coach's middleware stack.

### §81 — Standard content blocks (`content_blocks`)
- **Mismatch.** Neither doc mentions `content_blocks` (0 occurrences in `backend/`). CLAUDE.md §4.3
  / §11 say "never parse JSON from raw LLM text" but stop at structured-output binding, not typed
  blocks. §81 says new code should read `response.content_blocks`.
- **Change:** extend CLAUDE.md §4.3/§11 and ARCHITECTURE.md §3 to mandate `content_blocks` reading
  (also closes EDUCATIONAL Gap 33). The drift registry already forward-references a **CLAUDE.md §4.5**
  for this (pattern-3 message). Directly relevant to Delta 3.4 below.

### §82 — ProviderStrategy / `response_format=` (native structured output)
- **Sharpest contradiction.** CLAUDE.md **§4.3 is titled and worded to mandate** the
  `with_structured_output` binding ("the structured output binding is the only path", ~lines
  262–272). §82 says prefer `response_format=ProviderStrategy(...)` on `create_agent`. **The
  project's own drift hook already blocks that binding** (pattern-2) — so CLAUDE.md §4.3 currently
  mandates exactly what the hook forbids.
- **Dangling ref:** the drift hook (pattern-2 message) cites **CLAUDE.md §4.6**, which **does not
  exist** (CLAUDE.md §4 stops at §4.4). v2.2 must add §4.6 and soften/retire §4.3.
- **Change:** rewrite CLAUDE.md §4.3 → `response_format=`; add §4.6; update ARCHITECTURE.md §3.2.

### §83 — Agent Skills spec (runtime SKILL.md)
- **Mismatch.** ARCHITECTURE.md §5 has **no `skills/` directory** and no runtime-skills concept.
  §83 wants the five DMAIC phases as runtime skills under `agent-improve/skills/` via
  `FilesystemBackend`. (Distinct from the `.claude/` dev-time skills, which are unrelated.)
- **Change:** add a runtime-skills subsection to ARCHITECTURE.md §3/§5.

### §84 — SkillsMiddleware (deepagents)
- **Mismatch.** ARCHITECTURE.md §3.2 (lines 88–102) frames the coach as tool-binding via
  `bind_tools` + "true ReAct agent"; CLAUDE.md §5.2 mandates `bind_tools([...])`. §84 wants
  `create_deep_agent` + `SkillsMiddleware` + `create_agent`. `create_agent`/`create_deep_agent`
  appear 0 times in code.
- **Change:** update ARCHITECTURE.md §3.2 and CLAUDE.md §5.2 to the `create_agent` / SkillsMiddleware
  model (interacts with the §5 toolset reconciliation — see Delta 1d).

### §85 — LangSmith 2026 (Fleet, Sandboxes, Context Hub, Engine)
- **Mismatch (mostly deferred).** ARCHITECTURE.md §10 covers only basic tracing — no Sandboxes /
  Fleet / Context Hub / Engine. §85 itself marks Fleet, Context Hub, and Engine **not needed for
  v2.1**. **Sandboxes** (test isolation from prod Azure) is the one worth a §10 note now.
- **Change:** optional §10 note on Sandboxes for the test suite; defer the rest with a roadmap line.

---

## Delta 3 — Undocumented deviations (silent drift)

Neither documented as v2.1 target nor as v1 legacy. Ordered by severity.

**D3.1 — Class in a functions-only file (CLAUDE.md §2 / §11 violation).**
`core/llm.py:29` defines `class LLMProvider`. CLAUDE.md §2 explicitly lists `core/llm.py` (the LLM
factory) as **module-level functions ONLY**, and §11 says "never add classes outside the designated
files in §2." `llm.py` is a migrated/kept v2 file (not slated for deletion), so this is live drift,
not legacy. **Constitution violation.**

**D3.2 — LLM role taxonomy diverges from CLAUDE.md §4.2.**
`core/llm.py:19–26` `_ROLE_MAP` defines roles `intent, reasoning, operational, premium, extraction,
coach`. CLAUDE.md §4.2 defines `intent, reasoning, coach, extraction, reflection, vision`. The code
**adds** `operational`/`premium` and **drops** `reflection`/`vision` — with no ARCHITECTURE.md
amendment, which §4.2 requires for role changes. A `vision` role is absent despite the `upload/`
multimodal path existing.

**D3.3 — Deprecated LangGraph primitive in the live compiled graph.**
`core/graph.py:73` uses `set_entry_point`; lines 77/89/101/113/125 use conditional-edge routing
that the v2 target replaces with subgraph composition. `set_entry_point` is precisely the pattern
the repo's **own** drift hook now blocks for new writes (pattern-6) — grandfathered here because the
hook only guards new writes. graph.py is replaced in Step 5.1, but no doc records these as accepted
interim debt.

**D3.4 — Raw `.content` parsing in a file outside the migration sequence.**
`upload/agent.py:107` (`result.content.strip()`) parses raw model text. `upload/` is **not** covered
by any §15 step (Steps 0–9 never touch it), so unlike the `orchestrate.py` instances it will
**survive** the migration unaddressed. Contradicts §81 (use `content_blocks`) and CLAUDE.md §4.3/§11.
> For contrast, the *v1-legacy* `.content` parsing sites (scheduled for deletion/rewrite, and
> explicitly grandfathered by `path_exclusions` in the drift registry — **not** counted as silent
> drift): all five `phases/{…}/orchestrate.py` (e.g. define:174/230/243/308/357),
> `escalate.py:77` (rewritten Step 5.2), `gateway/routes.py:67` (rewritten Step 6.1).

**D3.5 — Tools without `args_schema`, and dead (CLAUDE.md §5.3 / §11 violation).**
All seven `@tool` functions in `knowledge/tools.py` use **raw signature inference** — none pass
`args_schema=`. CLAUDE.md §5.3 requires a Pydantic `args_schema` per tool; §11 says "no tools with
raw signature inference." Compounded by the toolset being the wrong set (Delta 1d) and imported
nowhere (Delta 1c). `search_flow_vsm` (tools.py:164–169) is a live stub.

**D3.6 — Gate mechanism contradicts the constitution.**
The running gate is a conditional-edge pass/fail loop (`core/graph.py:37–49, 77–133`), whereas
CLAUDE.md §1.6 and ARCHITECTURE.md §3.5 both assert an `interrupt`-based gate as the current design
(`interrupt` = 0 in code). Interim v1, but the docs present interrupt-gates as fact, not future.

**D3.7 — Client-level `max_retries=3` (low severity).**
`core/llm.py:48` sets `max_retries=3` on the Azure client — overlaps the `ModelRetryMiddleware`
pattern §80 recommends. Harmless today; note during the §80 middleware design so retry lives in one
place.

---

## Summary

| Metric | Count |
|---|---|
| **Total deltas** | **21** (Delta 1: 6 · Delta 2: 7 · Delta 3: 7 · precondition: 1) |
| **Blocking for Step 3.1 refactor resume** | **1** |
| **Blocking for v2.2 constitution rewrite** | **6** |
| **Deferred** | **6** |

**Blocking for Step 3.1 resume (1).** Step 3.1 writes `knowledge/tool_args.py` — Pydantic
`args_schema` for "all 7 tools." But the canonical 7-tool set (§5) does not exist in code; today's
`tools.py` is a different, unbound search-tool set (Delta 1c/1d, D3.5). **The universal toolset must
be reconciled (which 7 tools, which contracts) before their arg schemas can be written.** Design
inputs §82 and §84 (below) should be settled alongside it, since they shape the tool contracts.

**Blocking for v2.2 constitution rewrite (6):** §79, §80, §81, §82, §83, §84 — each requires new or
rewritten CLAUDE.md / ARCHITECTURE.md rules (see Delta 2). §82 is the priority: CLAUDE.md §4.3
mandates the pattern the drift hook blocks, and the hook's cited §4.6 does not yet exist. (The
registry also forward-references §3.6 and §4.5, which likewise do not yet exist.)

**Deferred (6):** DeltaChannel (§79.4, >200 turns), and from §85: Fleet, Context Hub, Engine
(enterprise scale) and — softer — Sandboxes (useful for test isolation but not required). The
`_Artifacts/` ignore-rule nit is deferred housekeeping (add `_Artifacts/` to `.gitignore` or rename
the folder to match the existing `ARTIFACTS/` rule).

**Overall posture.** The codebase is a coherent **end-of-Step-2 v1 system**: v2 tracing +
checkpointer are in place, but all five phases, the supervisor graph, the universal toolset, and the
interrupt gates remain v1. The larger risk for v2.2 is **not** the code drift (it is expected and
step-mapped) — it is that the v2.1 *constitution itself* is now behind EDUCATIONAL §§79–85 on six
counts, and in one case (§82 vs §4.3) actively self-contradictory with the drift hook shipped in
Part 1.
