# secrets
.env

# python
.venv/
__pycache__/
*.pyc

# OS / editor junk
.DS_Store
Thumbs.db

# LLM evaluation logs
logs/
*.jsonl

# Local working files � not part of the repo
# Each line must match the directory that ACTUALLY EXISTS. `ARTIFACTS/`
# matched nothing while `_Artifacts/` sat untracked in the tree: an ignore
# rule that matches nothing fails the same silent way a rule file's
# `paths:` glob does. CLAUDE.md §0.32 clause 2.
ARTIFACTS/
_Artifacts/
_Claude_chat_Prompts/
PROMPT_AUDIT.md
quick_test_langfuse.py
uvicorn_err.txt
uvicorn_out.txt
# .claude/ scaffold: track shared governance, ignore local-only overrides
.claude/settings.local.json
.claude/**/local/
.claude/**/.cache/
docs/CLAUDE.md
.claude/logs/
